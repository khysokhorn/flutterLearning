import 'package:flutter/material.dart';

class FavoriteContainerView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("favorite")),);
  }
}
