import 'package:flutter/material.dart';

class CartContainerView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("cart")),
    );
  }
}
